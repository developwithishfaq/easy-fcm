package com.easy_fcm.implementation

import com.easy_fcm.notifications.models.FcmNotification
import com.easy_fcm.notifications.models.FcmParams
import com.easy_fcm.notifications.models.SendType
import com.easy_fcm.notifications.utils.EasyFcm

class TestImpl {
    /*fun pushNotification() {
        val easyFcm = EasyFcm(serverKey = YOUR_SERVER_KEY)

        easyFcm.pushNotification(
            params = FcmParams(
                remoteNotificationData = FcmNotification(
                    title = NOTIFICATION_TITLE,
                    message = NOTIFICATION_MESSAGE
                ),
                dataParams = listOf(
                    Pair(KEY_1,KEY_1_VALUE),
                    Pair(KEY_2,KEY_2_VALUE)
                )
            ),
            sendType = SendType.Topic("All")
        )
    }
*/
}